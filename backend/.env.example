# Copy to ".env". Use the SERVICE ROLE key here (server-side only — never expose it in the frontend).
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
SUPABASE_SERVICE_KEY=YOUR-SERVICE-ROLE-KEY
PORT=4000
