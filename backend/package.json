{
  "name": "chaat-chaska-backend",
  "version": "1.0.0",
  "type": "module",
  "description": "Optional Node.js/Express API for Chaat Chaska. Supabase already exposes a REST API, so this is only needed for custom server logic (order webhooks, email, etc.).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
